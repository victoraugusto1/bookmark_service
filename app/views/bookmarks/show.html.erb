<p id="notice"><%= notice %></p>

<div class="col-xs-1" align="center">

  <table class="table">
    <thead>
        <tr>
          <th>Title</th>
          <td><%= @bookmark.title%></th>
        </tr>
        <tr>
          <th>URL</th>
          <td><%= @bookmark.url%></th>
        </tr>
        <tr>
          <th>Shortened URL</th>
          <td><%= @bookmark.shortened_url%></th>
        </tr>
    </thead>

  </table>
</div>

<%= link_to 'Edit', edit_bookmark_path(@bookmark) %> |
<%= link_to 'Back', bookmarks_path %>
